import React from "react";

const RollingSlice = (props) => {
  const { title, artist, overview, dimensions, statement, imgUrl } = props;
  return <div>RollingSlice</div>;
};

export default RollingSlice;
